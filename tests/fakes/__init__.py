"""Test doubles.

Fakes live only here. Production configuration refuses them unless AGTYLE_ENV=test or an
explicit demo profile is selected, so a real deployment can never silently degrade to one.
"""
